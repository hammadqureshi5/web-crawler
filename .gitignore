# Secrets / environment
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Build / test artifacts (regenerated)
build/
dist/
.pytest_cache/

# Scraper outputs (contain PII — do not commit)
scraper.log
scraper_*.log
results.csv
results_*.csv
results.xlsx
results_*.xlsx

# VPN baseline (your real IP — do not commit)
.vpn_baseline

# GUI remembered preferences (may contain proxy)
.gui_prefs.json

# Dedicated Chrome profile for the scraper (extensions, cookies, cache)
.chrome-profile/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Office lock / temp files
~$*
